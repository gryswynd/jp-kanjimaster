# Keep secrets, local-only, and junk OUT of the Cloud Run image.
# NOTE: curriculum/ is intentionally NOT ignored — it's generated by
# `npm run predeploy` (stage-curriculum.mjs) and must ship in the image.
node_modules
.env
*.local
service-account*.json
.build.log
.sync.log
smoke*.mjs
smoke*.sh
*.test.*
